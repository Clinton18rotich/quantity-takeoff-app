function renderWorkSections(){const container=document.getElementById('workSections');container.innerHTML=smmWorkSections.map(section=>`<div class="work-section-group"><div class="section-header" onclick="toggleSection(this)"><span class="expand-icon">▶</span> ${section.icon} ${section.title}</div><div class="section-items">${section.items.map(item=>`<label class="step-item"><input type="checkbox" value="${item.id}" data-section="${section.id}" onchange="updateSelection(this)"> ${item.desc} <small style="color:#999;margin-left:5px;">[${item.unit}]</small></label>`).join('')}</div></div>`).join('')}function toggleSection(header){header.classList.toggle('open');const items=header.nextElementSibling;items.classList.toggle('expanded')}function updateSelection(checkbox){const label=checkbox.parentElement;if(checkbox.checked){label.classList.add('selected')}else{label.classList.remove('selected')}}document.getElementById('addToSheetBtn').addEventListener('click',()=>{const checked=document.querySelectorAll('.step-item input:checked');if(checked.length===0){alert('Please select at least one work item.');return}const selectedItems=[];checked.forEach(cb=>{const section=smmWorkSections.find(s=>s.id===cb.dataset.section);if(section){const item=section.items.find(i=>i.id===cb.value);if(item)selectedItems.push(item)}});takeoffSheet.addItems(selectedItems)});document.getElementById('calculateBtn').addEventListener('click',()=>{takeoffSheet.calculateAll()});document.getElementById('exportBtn').addEventListener('click',()=>{takeoffSheet.exportSheet()});document.getElementById('drawingTitle').addEventListener('input',function(){document.getElementById('sheetDrawingRef').textContent='Drawing Ref: '+(this.value||'—')});document.getElementById('takeoffDate').valueAsDate=new Date();renderWorkSections();
